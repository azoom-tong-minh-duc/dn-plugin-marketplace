---
name: deep-plan
description: Lên plan cho task phức tạp bằng cách dùng skill scan-code (plugin dug-scan-code) để quét ảnh hưởng qua 3 layer FE/BE/DB trước khi thiết kế plan.
---

TODO: viết chi tiết quy trình gọi skill `scan-code` (plugin `dug-scan-code`, chạy song song 3 agent fe-scanner/be-scanner/db-scanner) để quét ảnh hưởng, cách tổng hợp kết quả quét thành plan, và format output cuối cùng tại đây.
